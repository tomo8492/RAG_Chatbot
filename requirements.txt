# ============================================================
#  RAG Chatbot - Python 依存パッケージ
#  インストール:  pip install -r requirements.txt
# ============================================================

# --- Web サーバ ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9      # ファイルアップロード受信用
python-dotenv>=1.0           # .env 読み込み
itsdangerous>=2.1            # セッションCookieの署名

# --- ローカルLLM (Ollama) ---
ollama>=0.3.0

# --- ベクトル検索 (RAG) ---
chromadb>=0.5.0
sentence-transformers>=2.7   # 埋め込みモデル(多言語E5)。torchを含む

# --- 文書ローダ / オフィスファイル生成(Code: 本物のファイル作成にも使用) ---
pymupdf>=1.24                # PDF 読み込み
python-docx>=1.1             # Word (.docx) 読み書き
openpyxl>=3.1                # Excel (.xlsx) 読み書き(数式・グラフ対応)
python-pptx>=0.6.23          # PowerPoint (.pptx) 読み書き
reportlab>=4.0               # PDF 生成(Code エージェントのファイル作成用)

# --- OCR(スキャン/画像PDF。任意) ---
# 既定の OCR_ENGINE=vlm は Ollama のビジョンモデルを使うため追加導入は不要
# (VLモデルを ollama pull するだけ)。下記は OCR_ENGINE=tesseract を使う場合のみ:
#   pillow>=10.0               # 画像処理(tesseract OCR用)
#   pytesseract>=0.3.10        # Tesseract ラッパ。別途 OS に tesseract 本体 + jpn/eng データが必要

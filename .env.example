# ============================================================
#  RAG Chatbot 設定ファイル(例)
#  このファイルを ".env" にコピーして編集してください。
#    cp .env.example .env
# ============================================================

# --- サーバ ---
# 0.0.0.0 にすると同じネットワークの他PCからアクセスできます。
# 自分のPCだけで使うなら 127.0.0.1 に変更してください。
HOST=0.0.0.0
PORT=8000

# --- 簡易パスワード認証 ---
# 値を設定するとログイン画面が出ます。空にすると認証なし(誰でもアクセス可)。
# 他PCに公開する場合は必ず設定してください。
CHAT_PASSWORD=

# セッションCookie署名用の秘密鍵。空なら自動生成して data/secret.key に保存します。
SECRET_KEY=

# --- Ollama (ローカルLLM) ---
OLLAMA_HOST=http://localhost:11434
# 既定の生成モデル。UIからも切り替えできます。
CHAT_MODEL=qwen3:8b

# --- 埋め込み (RAG用ベクトル化) ---
# sentence-transformers (既定) か ollama を選べます。
EMBED_BACKEND=sentence-transformers
# sentence-transformers の場合の既定モデル(日本語対応・軽量)
EMBED_MODEL=intfloat/multilingual-e5-small
# EMBED_BACKEND=ollama の場合は、ollama pull したモデル名(例: nomic-embed-text)を指定

# --- RAG パラメータ ---
RAG_TOP_K=5
CHUNK_SIZE=800
CHUNK_OVERLAP=120

# --- アップロード上限 (MB) ---
MAX_UPLOAD_MB=50

# --- データ保存先 ---
DATA_DIR=data

# --- 画面タイトル ---
APP_TITLE=社内文書アシスタント
